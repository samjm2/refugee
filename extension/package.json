{
  "name": "wayfinder-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Wayfinder Chrome Extension — pre-fills government benefit forms from your profile",
  "scripts": {
    "build": "esbuild src/background.ts src/content.ts src/popup.ts --bundle --outdir=dist --format=esm --platform=browser --target=chrome120",
    "watch": "esbuild src/background.ts src/content.ts src/popup.ts --bundle --outdir=dist --format=esm --platform=browser --target=chrome120 --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
